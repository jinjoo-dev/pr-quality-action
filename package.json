{
  "name": "pr-quality-action",
  "version": "1.0.0",
  "description": "GitHub Custom Action: PR diff 기반 TypeScript 타입체크 및 ESLint 실행",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && node --input-type=module -e \"import('./dist/index.js')\" || true",
    "bundle": "rm -rf action-dist && ncc build src/index.ts -o action-dist --source-map --no-cache && echo '{\"type\":\"commonjs\"}' > action-dist/package.json && cp node_modules/typescript/lib/lib*.d.ts action-dist/",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@typescript-eslint/eslint-plugin": "^8.56.1",
    "@typescript-eslint/parser": "^8.56.1",
    "eslint": "^9.0.0",
    "eslint-plugin-promise": "^7.2.1",
    "eslint-plugin-react-hooks": "^7.0.1",
    "glob": "^11.0.0",
    "typescript": "^5.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "@vercel/ncc": "^0.38.1"
  }
}
